name: wallet_mock
description: A package that holds all mock implementations for the wallet_app
version: 1.0.0

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  wallet_core:
    path: ../wallet_core
  rxdart: ^0.28.0

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.15
