name: wallet_core
description: A package that holds the api code generated by flutter_rust_bridge_codegen
version: 1.0.0

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

environment:
  sdk: ^3.0.0

dependencies:
  flutter_rust_bridge: ^2.11.1
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.6.1
  ffigen: ^18.1.0
  flutter_lints: ^6.0.0
  freezed: ^3.2.0
  test: ^1.26.2
