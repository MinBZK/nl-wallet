import 'package:flutter/material.dart';

class ParagraphSpacer extends StatelessWidget {
  const ParagraphSpacer({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 16);
  }
}
