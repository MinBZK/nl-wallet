import '../disclosure_request_repository.dart';

class CoreDisclosureRequestRepository implements DisclosureRequestRepository {
  @override
  Future<DisclosureRequest> getRequest(String sessionId) {
    // TODO: implement getRequest
    throw UnimplementedError();
  }
}
