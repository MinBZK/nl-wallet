[package]
name = "http_utils"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[lib]
doctest = false

[features]
# Allow Credential Issuer Identifiers to contain "http://" and allow the Origin type to contain "https://".
allow_insecure_url = []
# Include implementations for http clients
client = ["reqwest/gzip", "serde_with/base64"]
# Include implementations of the axum IntoResponse trait.
server = ["axum/json", "dep:axum-server", "dep:rustls", "dep:rustls-pki-types", "dep:tracing", "dep:futures", "serde_with/base64", "tower-http/cors"]
# Include code to use for testing purposes
test = []

[dependencies]
async-trait.workspace = true
cfg-if.workspace = true
base64.workspace = true
data-url.workspace = true
derive_more = { workspace = true, features = ["as_ref", "debug", "constructor"] }
http.workspace = true
itertools.workspace = true
mime.workspace = true
nutype = { workspace = true, features = ["regex", "serde"] }
regex.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
serde_with.workspace = true
strum = { workspace = true, features = ["derive"] }
thiserror.workspace = true
url = { workspace = true, features = ["serde"] }
x509-parser.workspace = true

utils.path = "../utils"

axum = { workspace = true, optional = true }
axum-server = { workspace = true, optional = true }
futures = { workspace = true, optional = true }
reqwest = { workspace = true, optional = true }
rustls = { workspace = true, optional = true }
rustls-pki-types = { workspace = true, optional = true }
tracing = { workspace = true, optional = true }
tower-http = { workspace = true, optional = true }

[dev-dependencies]
assert_matches.workspace = true
rstest.workspace = true
tokio = { workspace = true, features = ["macros", "rt"] }
toml.workspace = true
