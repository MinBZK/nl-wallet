[package]
name = "flutter_api_macros"
version.workspace = true
edition.workspace = true
description = "Support macros for flutter_api."

[lib]
proc-macro = true

[dependencies]
quote = "1.0.26"
syn = { version = "2.0", features = ["full"] }
