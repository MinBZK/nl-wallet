{
  "name": "revocation-portal-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.10",
    "allure-vitest": "^3.2.0",
    "vitest": "^4.0.0"
  }
}
