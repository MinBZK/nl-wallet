{% call header::header(t.xyz_title, "") %}
<main>
    <section>
        <h4>{{ t.xyz_open_account }}</h4>
        <h2>{{ t.xyz_identify_yourself }}</h2>
        {% call notification::notification(t.xyz_success, "", "") %}
    </section>
    <section>
        {% call attributes::attributes(attributes) %}
    </section>
    <button disabled><span>{{ t.next }}</span></button>
</main>
