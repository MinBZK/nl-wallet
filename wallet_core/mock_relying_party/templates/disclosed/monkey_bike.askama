{% let username = attributes|attribute("given_name") %}

{% call header::header("MonkeyBike", username) %}
<main>
    {% call notification::notification(format!("Welkom {username}"), "", "") %}
    {% call search::search("Zoek product...") %}
    {% call products::products(6) %}
</main>
