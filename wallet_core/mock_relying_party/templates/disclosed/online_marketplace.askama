{% let username = attributes|attribute("given_name") %}

{% call header::header(t.marketplace_title, username) %}
<main>
    {% call notification::notification(format!("{{ t.welcome }} {username}"), "", "") %}
    {% call search::search(t.search_by_topic) %}
    {% call products::products(6) %}
</main>
